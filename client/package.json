{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd src && browserify main.js -o ../dist/bundle.js",
    "dev": "cd dist && static-server",
    "tracker": "bittorrent-tracker -p 9081"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "drag-drop": "^4.2.0",
    "webtorrent": "^0.103.1"
  },
  "devDependencies": {
    "bittorrent-tracker": "^9.11.0",
    "static-server": "^2.2.1"
  }
}
